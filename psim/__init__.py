"""psim — scenario simulation, validation, and packaging for SDE state-space models.

The mandatory pre-SMC² stage of the modelling workflow:
  Public dev repo (model defs)  →  THIS repo (scenarios + validation)  →  SMC² repo
"""

__version__ = "0.1.1"
